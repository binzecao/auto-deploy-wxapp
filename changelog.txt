更新记录

V1.0.3
1.下载包支持https。

V1.0.2
1. 打开编辑器方式变成默认是命令行打开
2. 调整控制台的日志输入等级，默认是error，允许通过命令行加参数"--debug=1"启动服务端（"node ./index.js --debug=1"），来将等级变为debug。
3. 更新crx的一些样式。
4. 更新crx的icon。
5. 更新打开的页面，使得允许在新后台页面使用。

V1.0.1
1. 增加支持新后台的发布页面。
2. 服务端增加token校验，防止重复提交。
3. 服务端增加全局标识，用作控制只允许一个请求在执行流程，其他请求拒绝执行。
